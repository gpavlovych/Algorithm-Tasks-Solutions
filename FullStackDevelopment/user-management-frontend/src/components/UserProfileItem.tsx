import * as React from "react";
import {UserInfo} from "../services/UserService";

export class UserProfileItem extends React.Component<UserInfo> {
    render() {
        return <label>whatever</label>;
    }
}